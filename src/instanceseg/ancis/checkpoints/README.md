This folder contains saved model weights.

To train a model, please call the `train()` functions in the `train_detect.py`
and `train_seg.py` modules.
Trained weights will be saved to this directory by default.
